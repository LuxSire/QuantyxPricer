Simple React preview for priced bonds

Open `website/index.html` in a browser (or serve the project root) and the page will
fetch `../output/prices.json` and render a table showing key pricing fields.

Quick local serve from project root:

```bash
python -m http.server 8000
# then open http://localhost:8000/website/index.html
```
